_format_version: '3.0'
_transform: false
services:
- name: my-service
  url: https://example.com
  plugins:
  - name: proxy-cache
    config:
      strategy: redis
      content_type:
      - application/json; charset=utf-8
      redis:
        host: host.docker.internal
        port: 6379
  routes:
  - name: my-route
    paths:
    - /testroute